# Minimal Apache ports configuration
# Listen on port 8088
Listen 8088

# SSL ports (if SSL module is loaded)
<IfModule mod_ssl.c>
    Listen 443 ssl
</IfModule> 

<IfModule mod_gnutls.c>
    Listen 443 ssl
</IfModule>
